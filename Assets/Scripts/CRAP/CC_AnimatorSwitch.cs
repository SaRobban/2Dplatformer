using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CC_AnimatorSwitch : MonoBehaviour
{
    [SerializeField] private Animator animator;

    /*
    ChangeAnimation(string to)
    {
        switch (to)
        {
            case "Idle":
                    animator.Play("Idle");
                    break;
                case "Run"
                anima
        }

    }
    */
}
